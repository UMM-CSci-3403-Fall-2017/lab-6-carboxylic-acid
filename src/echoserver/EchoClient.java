package echoserver;
import java.net.*;
import java.io.*;

public class EchoClient {
	public static main(String[] args){
		try{
			//Opens a socket
			Socket Csocket = new Socket("127.0.0.1", 6013);
			
			InputStream thisone = new DataInputStream(System.thisone)
			//Define a stream for the byetes to be handled
			InputStream Sin = Csocket.getInputStream();
			OutputStream Sout = Csocket.getOutputStream();
			// This is a variable that hold data
			int a;
			while(check if infos exist){
				//write
				//clean stream
				//Maybe servertime?
			}
			//close
		}
		//catch
	}
}
